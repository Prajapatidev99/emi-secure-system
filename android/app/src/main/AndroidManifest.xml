<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    
    <!-- Telephony Permissions for SIM/IMEI Discovery -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    
    <!-- Notification Permissions for Payment Reminders -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    
    <!-- Location Permissions for Real-Time Tracking -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EMISecure"
        android:usesCleartextTraffic="false"
        android:directBootAware="true"
        tools:targetApi="31">

        <!-- 🔒 LOCK SCREEN (KIOSK) -->
        <activity
            android:name=".LockScreenActivity"
            android:theme="@style/Theme.EMISecure.FullScreen"
            android:exported="false"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:excludeFromRecents="true"
            android:showOnLockScreen="true"
            android:taskAffinity=""
            android:lockTaskMode="if_whitelisted"
            android:directBootAware="true" />

        <!-- 🏠 MAIN UI -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🔔 FIREBASE MESSAGING -->
        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- 🔒 LOCK MONITOR SERVICE -->
        <service
            android:name=".LockMonitorService"
            android:exported="false"
            android:foregroundServiceType="specialUse|location"
            android:directBootAware="true">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Device Security Monitoring" />
        </service>

        <!-- 🔐 LOCK SCREEN STICKINESS SERVICE (CRITICAL FIX #4) -->
        <service
            android:name=".LockScreenStickinessService"
            android:exported="false"
            android:directBootAware="true"
            android:foregroundServiceType="specialUse"
            android:label="@string/app_name">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Lock Screen Persistence" />
        </service>

        <!-- 🔐 DEVICE ADMIN -->
        <receiver
            android:name=".MyDeviceAdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true"
            android:label="@string/app_name"
            android:description="@string/device_admin_description">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_policies" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- 🔌 USB MONITOR (SECURITY) -->
        <receiver
            android:name=".UsbMonitorReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_STATE" />
            </intent-filter>
        </receiver>

        <!-- 📅 PAYMENT REMINDER RECEIVER (OFFLINE NOTIFICATIONS) -->
        <receiver
            android:name=".utils.PaymentReminderReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- 🔁 BOOT RECEIVER (DIRECT BOOT SAFE) -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.USER_UNLOCKED" />
                <action android:name="android.intent.action.REBOOT" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- 🔄 POST-RESET RE-PROVISIONING RECEIVER -->
        <!-- Detects fresh installs after factory reset and forces re-lock via server -->
        <receiver
            android:name=".PostResetReprovisionReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter android:priority="1000">
                <!-- Fires on normal boot (catches re-install on boot) -->
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <!-- Fires when APK is re-installed/updated on a running device -->
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
